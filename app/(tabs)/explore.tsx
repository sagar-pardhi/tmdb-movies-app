import React from "react";
import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function ExploreScreen() {
  return (
    <SafeAreaView>
      <View>
        <Text className="text-3xl">ExploreScreen</Text>
      </View>
    </SafeAreaView>
  );
}
